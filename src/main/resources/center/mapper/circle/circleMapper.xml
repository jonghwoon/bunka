<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cobra.mvc.center.circle.persistence.CircleDAO">
	<select id="MaxValue" parameterType="cobra.mvc.center.circle.domain.CircleDTO" resultType= "Integer">
 		SELECT Max(name) FROM ${tableName}
 	</select>
</mapper>
